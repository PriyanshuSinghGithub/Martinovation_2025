/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  darkMode: 'media', // Use system preference, but we'll override with CSS
  theme: {
    extend: {},
  },
  plugins: [],
};
